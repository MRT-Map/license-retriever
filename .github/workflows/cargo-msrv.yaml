on:
  workflow_dispatch:
  push:
    paths: ["**/Cargo.toml", "**/Cargo.lock"]
    
jobs:
  cargo-msrv:
    uses: iiiii7d/cargo-msrv-workflow/.github/workflows/workflow.yml@cbce077750b4f0477f29a63420915b330db645bd # main
    permissions:
      contents: write
      pull-requests: write
