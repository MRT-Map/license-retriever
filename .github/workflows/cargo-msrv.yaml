on:
  workflow_dispatch:
  push:
    paths: ["**/Cargo.toml", "**/Cargo.lock"]
    
jobs:
  cargo-msrv:
    uses: iiiii7d/cargo-msrv-workflow/.github/workflows/workflow.yml@b068dfa6a1a77a3fc83403d96029b8172fdfad25 # main
    permissions:
      contents: write
      pull-requests: write
