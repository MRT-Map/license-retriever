on:
  workflow_dispatch:
  push:
    paths: ["**/Cargo.toml", "**/Cargo.lock"]
    
jobs:
  cargo-msrv:
    uses: iiiii7d/cargo-msrv-workflow/.github/workflows/workflow.yml@dba699327b2c2c3434f1708a89cf711df5dae200 # main
    permissions:
      contents: write
      pull-requests: write
