name: Run cargo-msrv
on:
  workflow_dispatch:
  push:
    branches: ["**"]
    paths: ["**/Cargo.toml", "**/Cargo.lock"]
    
jobs:
  cargo-msrv:
    uses: iiiii7d/cargo-msrv-workflow/.github/workflows/workflow.yml@bd28963549ce143a5cb42a432966eb0f70067090 # main
    permissions:
      contents: write
      pull-requests: write
