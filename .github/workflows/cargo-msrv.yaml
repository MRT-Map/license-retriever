name: Run cargo-msrv
on:
  workflow_dispatch:
  push:
    branches: ["**"]
    paths: ["**/Cargo.toml", "**/Cargo.lock"]
    
jobs:
  cargo-msrv:
    uses: iiiii7d/cargo-msrv-workflow/.github/workflows/workflow.yml@bf7fd621d2d0e36481249fc34cf2dd6d6e22c057 # main
    permissions:
      contents: write
      pull-requests: write
