name: Run cargo-msrv
on:
  workflow_dispatch:
  push:
    branches: ["**"]
    paths: ["**/Cargo.toml", "**/Cargo.lock"]
    
jobs:
  cargo-msrv:
    uses: iiiii7d/cargo-msrv-workflow/.github/workflows/workflow.yml@be132d2eee81096484a2d93a0bf77727963a3859 # main
    permissions:
      contents: write
      pull-requests: write
