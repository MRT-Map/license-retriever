on:
  workflow_dispatch:
  push:
    paths: ["**/Cargo.toml", "**/Cargo.lock"]
    
jobs:
  cargo-msrv:
    uses: iiiii7d/cargo-msrv-workflow/.github/workflows/workflow.yml@c93c75569b5c5def7065c7ad5b854c129dd2e39d # main
    permissions:
      contents: write
      pull-requests: write
